# Example environment variables for NRSH

# Backend API base URL used by the frontend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Axios timeout in milliseconds for frontend requests
# Default used in code: 15000 (15 seconds)
NEXT_PUBLIC_AXIOS_TIMEOUT_MS=15000

# Polling configuration for analysis status
# Interval between polling requests (ms)
NEXT_PUBLIC_POLL_INTERVAL_MS=2000
# Maximum number of polling retries before timing out
NEXT_PUBLIC_POLL_MAX_RETRIES=60

# Default map center coordinates used by MapVisualization when analysis has no bounds
NEXT_PUBLIC_MAP_DEFAULT_LAT=12.9716
NEXT_PUBLIC_MAP_DEFAULT_LON=77.5946

# (Optional) Any other runtime environment variables used by the project
# e.g., SUPABASE_URL, SUPABASE_KEY, etc.
# SUPABASE_URL=
# SUPABASE_KEY=

# Example backend/runtime variables you might need when running the API locally
# These are typically set in the environment where the backend FastAPI runs.
# CONFIG_PATH points to the YAML config used by the app (optional override):
# CONFIG_PATH=config/config.yaml

# Supabase / database credentials (if using Supabase for persistence).
# Only set these if you are using the Supabase-backed services in the project.
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-public-or-secret-key

## Central list of commonly used environment variables
## Variable : purpose | type | default | consumed by
# NEXT_PUBLIC_API_URL : Frontend API base URL | string | http://localhost:8000 | frontend (axios calls)
# NEXT_PUBLIC_AXIOS_TIMEOUT_MS : Frontend request timeout (ms) | int | 15000 | frontend (axios)
# NEXT_PUBLIC_POLL_INTERVAL_MS : Analysis polling interval (ms) | int | 2000 | frontend (VideoUpload polling)
# NEXT_PUBLIC_POLL_MAX_RETRIES : Max polling retries | int | 60 | frontend (VideoUpload polling)
# NEXT_PUBLIC_MAP_DEFAULT_LAT : Default map center latitude | float | 12.9716 | frontend (MapVisualization)
# NEXT_PUBLIC_MAP_DEFAULT_LON : Default map center longitude | float | 77.5946 | frontend (MapVisualization)
# CONFIG_PATH : Path to YAML config (optional) | string | config/config.yaml | backend (ConfigManager constructor)
# SUPABASE_URL : Supabase project URL | string | (none) | backend services (database)
# SUPABASE_KEY : Supabase anon/secret key | string | (none) | backend services (database)

# NOTE: For more details and guidance on setting environment variables for
# local development and deployment, see the top-level `README.md` environment
# documentation and this file as an example.
